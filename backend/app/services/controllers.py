class ControllerService:
    stream_host = ''

    def handle_request(self, app):
        type = ''

        if type == 'gatekeeper':
            return

        if type == 'openalpr':
            return

    def send_command(self, command = 'open'):
        type = ''

        if type == 'gatekeeper':
            return

        if type == 'openalpr':
            return

    def get_status(self):
        type = ''

        if type == 'gatekeeper':
            return

        if type == 'openalpr':
            return
